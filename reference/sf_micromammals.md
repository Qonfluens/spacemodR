# DataBase of collected MicroMammals species

DataBase of collected MicroMammals species

## Usage

``` r
data(sf_micromammals)
```

## Format

An object of class `sf` (inherits from `tbl_df`, `tbl`, `data.frame`)
with 1426 rows and 27 columns.

## Examples

``` r
data(sf_micromammals)
```
