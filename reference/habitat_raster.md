# Create a raster from a habitat object

Create a raster from a habitat object

## Usage

``` r
habitat_raster(ground_raster, habitat)
```

## Arguments

- ground_raster:

  SpatRaster (reference grid).

- habitat:

  habitat object inherited from `sf` data.frame class.

## Value

SpatRaster (weight and NA in nogo)
