# SF object defining ROI metaleurop

Simple feature collection with 1 feature and 1 field. Geodetic CRS: WGS
84.

## Usage

``` r
data(roi_metaleurop)
```

## Format

An object of class `sf` (inherits from `data.frame`) with 1 rows and 2
columns.

## Examples

``` r
data(roi_metaleurop)
```
